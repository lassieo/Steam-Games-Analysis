# Tasks Remaining


### 1. Modeling
Build a baseline classification model to predict `success` using the current project definition as-is.

### 2. Tuning
After a baseline model is established, test tuned versions of logistic regression, random forest, gradient boosting, or XGBoost-style models. Evaluation should compare accuracy, precision, recall, F1, and ROC-AUC.

### 3. Evaluation
Compare model performance with standard metrics such as accuracy, precision, recall, F1, and ROC-AUC so the results are easy to explain in the final report.

### 4. Clustering
Run clustering to identify natural game segments, such as low-price niche titles, high-engagement premium titles, or free games with strong review traction. This can add interpretation beyond binary success prediction.

### 5. Stronger reporting visuals
Add clean charts for review-count bins, price bins, and feature distributions so the final presentation is easier to follow.

## Short version
The next phase should move from descriptive EDA into modeling, tuning, clustering, and clearer final presentation while keeping the current success definition fixed.
